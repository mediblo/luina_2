riot.py:192 에서 match ID 요청 뒤에도 api_data == "RATE_LIMITED"를 검사하고 있어, rate limit 응답을 놓친 채 후속 로직이 잘못 진행될 수 있습니다.
lostark.py:100 근처의 날짜 파서는 tzinfo가 있는 ISO 문자열을 받으면 None을 반환해서, 종료일이 있는 이벤트를 상시 이벤트로 잘못 분류할 수 있습니다.

치명적: lifecycle.py:3가 services를 패키지 초기화로 전제하고 있는데, 현재 트리 기준 파일명이 표준 패턴이 아니라서 from services import shutdown 자체가 import 단계에서 실패할 가능성이 큽니다. 이 상태면 main 진입부터 막힐 수 있습니다.
치명적: firebase.py:13와 firebase.py:20는 logger.py:25 같은 래퍼가 아니라 raw logger에 log_info를 호출합니다. raw logging.Logger에는 그런 메서드가 없어서, services.log_service를 통해 firebase 모듈이 import되는 순간 바로 예외가 날 수 있습니다.
높음: main.py:21와 main.py:69도 같은 logger 오용을 반복합니다. 설령 import가 통과해도 on_ready나 앱 커맨드 에러 처리 시점에 터질 수 있고, main.py:27의 signal handler 등록은 Windows에서 호환성 문제가 생길 수 있습니다. on_ready마다 flush_task를 새로 띄우는 구조라 재접속 시 백그라운드 task가 중복될 여지도 있습니다.
높음: riot.py:185에서 매치 ID를 받아온 뒤에도 if api_data == "RATE_LIMITED"를 검사합니다. 여기서는 matchs를 검사해야 해서, 레이트리밋 상황에서 문자열을 match ID 목록처럼 순회하는 잘못된 흐름으로 들어갈 수 있습니다.
중간: lostark.py:104의 parse_end_date는 tzinfo가 있는 ISO 문자열이면 return이 없어 None을 돌려줍니다. 실제 종료일 데이터가 시간대 포함으로 오면 이벤트가 조용히 누락됩니다.
중간: api.py:54와 api.py:174는 외부 API 호출을 먼저 하고 defer를 나중에 합니다. 응답이 조금만 느려도 Discord 3초 제한을 넘길 수 있고, maplestory.py:30는 developer 캐시가 없을 때 await developer.send에서 바로 터질 수 있습니다.

지금 상태에서 가장 먼저 막아야 하는 건 startup/import blocker 3건